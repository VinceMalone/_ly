/**
 * A function that returns nothing and does nothing; it's a common utility though.
 *
 * ## Example
 * ```javascript
 * noop(); // undefined
 * ```
 *
 * @returns {undefined}
 */
export const noop = () => undefined;
